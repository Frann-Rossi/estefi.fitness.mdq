---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Estefi Fitness · Tarifario">
	<div id="toast"></div>

	<div class="modal-overlay" id="modal-overlay">
		<div class="modal-box">
			<div class="modal-drag-handle"></div>
			<h2 class="modal-title accent" id="modal-title">ACCESO ADMIN</h2>
			<p class="modal-subtitle" id="modal-subtitle">
				Ingresá tu contraseña para editar los precios.
			</p>

			<div id="section-login">
				<label class="field-label">Email</label>
				<input
					class="modal-input"
					type="email"
					id="input-email"
					placeholder="entrenador@ejemplo.com"
					autocomplete="username"
				/>
				<label class="field-label" style="margin-top:12px">Contraseña</label>
				<input
					class="modal-input"
					type="password"
					id="input-pass"
					placeholder="••••••••••"
					autocomplete="current-password"
					onkeydown="if(event.key==='Enter') window.checkPass()"
				/>
				<p class="modal-error" id="modal-error"></p>
				<div class="modal-btn-row">
					<button class="btn-primary" onclick="window.checkPass()" id="btn-login-submit"
						>Ingresar</button
					>
				</div>
			</div>


		</div>
	</div>

	<div class="page">
		<div class="header-eyebrow fade-up">
			<span class="badge">Mar del Plata</span>
			<span class="status-dot loading" id="status-dot"></span>
			<span class="status-text" id="status-text">Cargando precios…</span>
		</div>

		<h1 class="main-title fade-up d1">
			PERSONAL<br /><span class="accent">TRAINER</span>
		</h1>
		<p class="subtitle fade-up d2">Clases 1 a 1 · Tarifario</p>

		<div class="controls fade-up d2">
			<button
				class="btn-primary"
				id="btn-edit"
				onclick="window.openModal()">🔒 Acceso Admin</button
			>
			<button
				class="btn-outline hidden"
				id="btn-save"
				onclick="window.saveAll()">💾 Guardar</button
			>
			<button
				class="btn-ghost hidden"
				id="btn-reset"
				onclick="window.resetDefaults()">↺ Restaurar</button
			>
			<button
				class="btn-ghost hidden"
				id="btn-logout"
				onclick="window.doLogout()"
				style="margin-left:auto">🚪 Salir</button
			>
		</div>

		<div class="edit-banner">
			✏️ Modo edición activo — tocá cualquier valor para modificarlo
		</div>

		<p class="section-label fade-up d2">Clase individual</p>
		<div class="price-card fade-up d3">
			<div class="card-header">
				<div><h2 class="card-title accent">CLASE SUELTA</h2></div>
			</div>
			<div class="price-grid">
				<div class="price-item">
					<span class="price-label">Gym</span><span
						class="price-value"
						><span class="currency">$</span><span
							class="editable skeleton"
							id="suelta_gym"
							contenteditable="false">5.000</span
						></span
					>
				</div>
				<div class="price-item">
					<span class="price-label">Mi casa</span><span
						class="price-value"
						><span class="currency">$</span><span
							class="editable skeleton"
							id="suelta_casa"
							contenteditable="false">8.000</span
						></span
					>
				</div>
				<div class="price-item">
					<span class="price-label">Domicilio</span><span
						class="price-value"
						><span class="currency">$</span><span
							class="editable skeleton"
							id="suelta_dom"
							contenteditable="false">10.000</span
						></span
					>
				</div>
			</div>
		</div>

		<p class="section-label fade-up d3" style="margin-top:28px">
			Paquetes mensuales
		</p>

		<div class="price-card fade-up d3">
			<div class="card-header">
				<div>
					<h2 class="card-title">2 VECES / SEMANA</h2><p
						class="card-meta"
					>
						<span
							class="editable skeleton"
							id="pack2_clases"
							contenteditable="false">8</span
						> clases al mes
					</p>
				</div>
				<span class="off-badge"
					><span
						class="editable skeleton"
						id="pack2_off"
						contenteditable="false">10</span
					>% OFF</span
				>
			</div>
			<div class="price-grid">
				<div class="price-item">
					<span class="price-label">Gym</span><span
						class="price-value"
						><span class="currency">$</span><span
							class="editable skeleton"
							id="pack2_gym"
							contenteditable="false">36.000</span
						></span
					>
				</div>
				<div class="price-item">
					<span class="price-label">Mi casa</span><span
						class="price-value"
						><span class="currency">$</span><span
							class="editable skeleton"
							id="pack2_casa"
							contenteditable="false">57.600</span
						></span
					>
				</div>
				<div class="price-item">
					<span class="price-label">Domicilio</span><span
						class="price-value"
						><span class="currency">$</span><span
							class="editable skeleton"
							id="pack2_dom"
							contenteditable="false">72.000</span
						></span
					>
				</div>
			</div>
		</div>

		<div class="price-card fade-up d4">
			<div class="card-header">
				<div>
					<h2 class="card-title">3 VECES / SEMANA</h2><p
						class="card-meta"
					>
						<span
							class="editable skeleton"
							id="pack3_clases"
							contenteditable="false">12</span
						> clases al mes
					</p>
				</div>
				<span class="off-badge"
					><span
						class="editable skeleton"
						id="pack3_off"
						contenteditable="false">15</span
					>% OFF</span
				>
			</div>
			<div class="price-grid">
				<div class="price-item">
					<span class="price-label">Gym</span><span
						class="price-value"
						><span class="currency">$</span><span
							class="editable skeleton"
							id="pack3_gym"
							contenteditable="false">51.000</span
						></span
					>
				</div>
				<div class="price-item">
					<span class="price-label">Mi casa</span><span
						class="price-value"
						><span class="currency">$</span><span
							class="editable skeleton"
							id="pack3_casa"
							contenteditable="false">81.600</span
						></span
					>
				</div>
				<div class="price-item">
					<span class="price-label">Domicilio</span><span
						class="price-value"
						><span class="currency">$</span><span
							class="editable skeleton"
							id="pack3_dom"
							contenteditable="false">102.000</span
						></span
					>
				</div>
			</div>
		</div>

		<div class="price-card featured fade-up d5">
			<div class="card-header">
				<div>
					<h2 class="card-title accent">5 VECES / SEMANA</h2><p
						class="card-meta"
					>
						<span
							class="editable skeleton"
							id="pack5_clases"
							contenteditable="false">20</span
						> clases al mes
					</p>
				</div>
				<span class="off-badge"
					><span
						class="editable skeleton"
						id="pack5_off"
						contenteditable="false">20</span
					>% OFF</span
				>
			</div>
			<div class="price-grid">
				<div class="price-item">
					<span class="price-label">Gym</span><span
						class="price-value"
						><span class="currency">$</span><span
							class="editable skeleton"
							id="pack5_gym"
							contenteditable="false">80.000</span
						></span
					>
				</div>
				<div class="price-item">
					<span class="price-label">Mi casa</span><span
						class="price-value"
						><span class="currency">$</span><span
							class="editable skeleton"
							id="pack5_casa"
							contenteditable="false">128.000</span
						></span
					>
				</div>
				<div class="price-item">
					<span class="price-label">Domicilio</span><span
						class="price-value"
						><span class="currency">$</span><span
							class="editable skeleton"
							id="pack5_dom"
							contenteditable="false">160.000</span
						></span
					>
				</div>
			</div>
		</div>

		<div class="divider fade-up d5"></div>
		<div class="fade-up d5">
			<p class="section-label">Aclaraciones</p>
			<ul class="notas-list">
				<li>
					<span class="editable" id="nota1" contenteditable="false"
						>El valor del gym se abona aparte.</span
					>
				</li>
				<li>
					<span class="editable" id="nota2" contenteditable="false"
						>Pago mensual adelantado.</span
					>
				</li>
				<li>
					<span class="editable" id="nota3" contenteditable="false"
						>Clases no tomadas no se recuperan.</span
					>
				</li>
			</ul>
		</div>
		<p class="footer">Estefi Fitness · Personal Trainer · Mar del Plata</p>
	</div>

	<script>
		// @ts-nocheck
		import { getDoc, setDoc, onSnapshot } from "firebase/firestore";
		import { signInWithEmailAndPassword, signOut, onAuthStateChanged } from "firebase/auth";
		import { REF, auth } from "../lib/firebase";

		const KEYS = [
			"suelta_gym",
			"suelta_casa",
			"suelta_dom",
			"pack2_clases",
			"pack2_off",
			"pack2_gym",
			"pack2_casa",
			"pack2_dom",
			"pack3_clases",
			"pack3_off",
			"pack3_gym",
			"pack3_casa",
			"pack3_dom",
			"pack5_clases",
			"pack5_off",
			"pack5_gym",
			"pack5_casa",
			"pack5_dom",
			"nota1",
			"nota2",
			"nota3",
		];

		const DEFAULTS = {
			suelta_gym: "5.000",
			suelta_casa: "8.000",
			suelta_dom: "10.000",
			pack2_clases: "8",
			pack2_off: "10",
			pack2_gym: "36.000",
			pack2_casa: "57.600",
			pack2_dom: "72.000",
			pack3_clases: "12",
			pack3_off: "15",
			pack3_gym: "51.000",
			pack3_casa: "81.600",
			pack3_dom: "102.000",
			pack5_clases: "20",
			pack5_off: "20",
			pack5_gym: "80.000",
			pack5_casa: "128.000",
			pack5_dom: "160.000",
			nota1: "El valor del gym se abona aparte.",
			nota2: "Pago mensual adelantado.",
			nota3: "Clases no tomadas no se recuperan.",
		};

		const $ = (id) => document.getElementById(id);

		function applyData(data) {
			KEYS.forEach((k) => {
				const el = $(k);
				if (!el) return;
				el.textContent =
					data && data[k] !== undefined ? data[k] : DEFAULTS[k];
				el.classList.remove("skeleton");
			});
		}

		function setStatus(online) {
			const dot = $("status-dot");
			const text = $("status-text");
			if (!dot || !text) return;
			if (online) {
				dot.classList.remove("loading");
				text.textContent = "Precios actualizados";
			} else {
				dot.classList.add("loading");
				text.textContent = "Sin conexión";
			}
		}

		onSnapshot(
			REF,
			(snap) => {
				applyData(snap.exists() ? snap.data() : null);
				setStatus(true);
			},
			() => {
				setStatus(false);
			},
		);

		onAuthStateChanged(auth, (user) => {
			if (user) {
				// We don't auto-activate edit mode to avoid confusion, 
				// but when they click 'Editar precios', it will skip the modal.
				$("btn-logout").classList.remove("hidden");
				if (!editing) {
					$("btn-edit").textContent = "✏️ Editar precios";
				}
			} else {
				if (editing) cancelEdit();
				$("btn-logout").classList.add("hidden");
				$("btn-edit").textContent = "🔒 Acceso Admin";
			}
		});

		window.openModal = function () {
			if (auth.currentUser) {
				activateEdit();
				return;
			}
			$("section-login").classList.remove("hidden");
			$("modal-title").textContent = "ACCESO ADMIN";
			$("modal-subtitle").textContent =
				"Ingresá tu correo y contraseña para editar los precios.";
			$("input-email").value = "";
			$("input-pass").value = "";
			$("modal-error").textContent = "";
			$("btn-login-submit").textContent = "Ingresar";
			$("modal-overlay").classList.add("open");
			setTimeout(() => $("input-email").focus(), 280);
		};

		window.closeModal = function () {
			$("modal-overlay").classList.remove("open");
		};

		window.checkPass = async function () {
			const email = $("input-email").value;
			const inp = $("input-pass");
			const btn = $("btn-login-submit");
			const err = $("modal-error");
			
			if(!email || !inp.value) {
				err.textContent = "Ingresá email y contraseña.";
				return;
			}

			btn.textContent = "Verificando...";
			btn.disabled = true;

			try {
				await signInWithEmailAndPassword(auth, email, inp.value);
				window.closeModal();
				activateEdit();
			} catch(e) {
				inp.classList.add("shake");
				err.textContent = "Credenciales incorrectas.";
				setTimeout(() => inp.classList.remove("shake"), 400);
			} finally {
				btn.disabled = false;
				btn.textContent = "Ingresar";
				inp.value = "";
			}
		};

		window.doLogout = async function () {
			try {
				await signOut(auth);
				window.showToast("🚪 Sesión cerrada");
				cancelEdit();
			} catch(e) {
				window.showToast("⚠️ Error al salir");
			}
		};

		let editing = false;

		function activateEdit() {
			editing = true;
			document.body.classList.add("edit-mode");
			KEYS.forEach((k) => {
				const el = $(k);
				if (el) el.contentEditable = "true";
			});
			$("btn-edit").textContent = "✕ Cancelar";
			$("btn-edit").onclick = window.cancelEdit;
			$("btn-save").classList.remove("hidden");
			$("btn-reset").classList.remove("hidden");
			$("btn-logout").classList.remove("hidden");
		}

		window.cancelEdit = function () {
			editing = false;
			document.body.classList.remove("edit-mode");
			KEYS.forEach((k) => {
				const el = $(k);
				if (el) el.contentEditable = "false";
			});
			if (auth.currentUser) {
				$("btn-edit").textContent = "✏️ Editar precios";
			} else {
				$("btn-edit").textContent = "🔒 Acceso Admin";
			}
			$("btn-edit").onclick = window.openModal;
			$("btn-save").classList.add("hidden");
			$("btn-reset").classList.add("hidden");
			if (!auth.currentUser) {
				$("btn-logout").classList.add("hidden");
			}
			getDoc(REF)
				.then((snap) => applyData(snap.exists() ? snap.data() : null))
				.catch(() => {});
		};

		window.saveAll = async function () {
			const data = {};
			KEYS.forEach((k) => {
				const el = $(k);
				if (el) data[k] = el.textContent.trim();
			});
			try {
				await setDoc(REF, data);
				window.showToast("✓ Precios guardados");
				window.cancelEdit();
			} catch (e) {
				window.showToast("⚠️ Error al guardar.");
			}
		};

		window.resetDefaults = async function () {
			if (
				!confirm(
					"¿Restaurar todos los precios a los valores originales?",
				)
			)
				return;
			try {
				await setDoc(REF, DEFAULTS);
				window.showToast("↺ Valores restaurados");
				window.cancelEdit();
			} catch (e) {
				window.showToast("⚠️ Error al restaurar.");
			}
		};

		window.showToast = function (msg) {
			const t = $("toast");
			t.textContent = msg;
			t.classList.add("show");
			setTimeout(() => t.classList.remove("show"), 2800);
		};

		// Modal click outside
		document.addEventListener("DOMContentLoaded", () => {
			$("modal-overlay")?.addEventListener("click", (e) => {
				if (e.target === $("modal-overlay")) window.closeModal();
			});
		});
	</script>
</Layout>
